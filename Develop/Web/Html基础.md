---
source_title: Html基础
categories:
- Develop
- Web
last_modified: '2025-07-18T01:41:11Z'
---
超文本标记语言（HyperText Markup Language，简称：HTML）是一种用于创建网页的标准标记语言。

HTML 运行在浏览器上，由浏览器来解析。

### Head

#### 页面标题
 ```
  牛奶河BBS - Milky Way BBS
```

#### 页面图标
 ```
  
```

#### 描述
 ```
  
```

### Body

#### 在鼠标悬停时显示完整的内容
 ```
  短信息
```

一般情况下，会用在 CSS 中定义 info 限制显示的宽度（超过显示...)，这种情况下，title 标签就可以在鼠标悬停时显示完整的内容。只是纯文本，不支持   

 及类似的换行。
